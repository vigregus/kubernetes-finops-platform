{{- if .Values.postgres }}
# Postgres for analytics' order_events fact table - the row-per-consumed-event
# history that analytics-worker writes on its real consume path (see
# apps/analytics/app.py init_db/persist_event).
#
# Deliberately a separate cluster from checkout-db rather than another table
# in it: checkout-db backs live request traffic, and a table meant to grow to
# a size where storage engine choice matters has no business sharing an
# instance with it. Separation also attributes the storage cost to insights/
# analytics, which is whose data this is, rather than to commerce/checkout.
#
# See charts/app1/templates/postgres.yaml for the full reasoning behind
# inheritedMetadata and the honorLabels setting below - both apply verbatim.
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ .Values.postgres.clusterName }}
  namespace: {{ .Values.namespace }}
  labels:
{{ toYaml .Values.labels | indent 4 }}
spec:
  # Only the finops.internal/* keys: copying app.kubernetes.io/name would make
  # these pods match the analytics Deployment's own selector.
  inheritedMetadata:
    labels:
      finops.internal/product: {{ index .Values.labels "finops.internal/product" | quote }}
      finops.internal/team: {{ index .Values.labels "finops.internal/team" | quote }}
      finops.internal/environment: {{ index .Values.labels "finops.internal/environment" | quote }}
      finops.internal/component: {{ index .Values.labels "finops.internal/component" | quote }}
  instances: {{ .Values.postgres.instances }}
  storage:
    size: {{ .Values.postgres.storageSize }}
  postgresql:
    parameters:
      # Unlike checkout-db this does not log every write: order_events takes
      # one INSERT per consumed order, so log_statement=mod here would turn
      # the load profile into a log-volume test.
      log_min_duration_statement: "200"
  bootstrap:
    initdb:
      database: {{ .Values.postgres.database }}
      owner: {{ .Values.postgres.owner }}
  resources:
{{ toYaml .Values.postgres.resources | indent 4 }}
---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: {{ .Values.postgres.clusterName }}-metrics
  namespace: {{ .Values.namespace }}
  labels:
{{ toYaml .Values.labels | indent 4 }}
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: {{ .Values.postgres.clusterName }}
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 15s
      # CNPG's own "cluster" label collides with the cluster external_label
      # every scrape target is stamped with; without this the CloudNativePG
      # dashboard's $cluster variable finds only "local-v1".
      honorLabels: true
{{- end }}
