import { defineConfig } from "@playwright/test"

/**
 * Конфиг браузерной приёмки: G3-005 (вход, список бесед), G3-006 (пять
 * состояний соединения, обнаружение пропуска, синхронизация) и G3-007
 * (квитанции, непрочитанные, несколько вкладок, присутствие).
 *
 * `testDir` смотрит за пределы `apps/web`, и это не небрежность: спека живёт
 * в `tests/e2e/` рядом с остальными проверками. Раннер при этом берётся из
 * `apps/web/node_modules` — ради этого рядом лежит `tests/e2e/tsconfig.json`
 * с картой путей (в репозитории нет корневого `node_modules`, и без карты
 * спека не находит `@playwright/test` вовсе).
 *
 * Адреса стенда сюда не вписаны: их задаёт `scripts/web-e2e-fixture.sh`
 * переменными окружения. Приёмка без оркестратора не запускается — и падает
 * внятно, а не «Invalid URL»: `E2E_*` читаются проверяемо в самих тестах.
 */
export default defineConfig({
	testDir: "../../tests/e2e",
	// Одна вкладка за раз — и довод здесь **не** тот, что стоял тут раньше.
	//
	// Прежняя редакция утверждала: «второй одновременный вход с той же записью
	// снял бы сессию первого». Это не измерено: ближайшее к проверке —
	// `ensure_session`, идемпотентная по `session_id`, выведенному из `sid`
	// токена (`repositories/sessions.py`), то есть вход той же записью не
	// заводит вторую сессию, а находит ту же. Утверждение снято, а не
	// унаследовано: комментарий, называющий догадку правилом, стоит дороже
	// отсутствующего.
	//
	// Измерено другое, и этого достаточно: две страницы **одного** контекста
	// живут одновременно — обе остаются `connected`, запрос первой продолжает
	// отвечать `200` (спека `g3-007`, шаг «вкладки уложены»). А параллельность
	// раннера запрещает не это, а состояние прогона: файл `E2E_STATE_FILE`
	// один, его пишет проект `fixture`, и два воркера писали бы в него врозь.
	workers: 1,
	fullyParallel: false,
	forbidOnly: Boolean(process.env.CI),
	retries: 0,
	reporter: [["list"]],
	// Артефакты — внутрь `node_modules`: каталог по умолчанию (`test-results/`)
	// оказался бы в рабочем дереве репозитория.
	outputDir: "node_modules/.cache/playwright-artifacts",
	timeout: 120_000,
	expect: { timeout: 20_000 },
	use: {
		baseURL: process.env.E2E_BASE_URL,
		// Локальный удостоверяющий центр стенда: сертификаты выписаны им, и
		// браузеру в приёмке он не доверяет. Имена при этом настоящие —
		// подмены адреса нет, только доверие к сертификату.
		ignoreHTTPSErrors: true,
		trace: "retain-on-failure",
	},
	projects: [
		// Подготовка и приёмка — разные работы: первая заводит фикстуры и беседу
		// и пишет состояние в файл, вторая читает это состояние. Проект
		// `acceptance` зависит от `fixture`, поэтому порядок задан раннером, а не
		// порядком строк в спеке.
		//
		// `fixture.setup.ts` не импортируется спекой намеренно: модуль с `test()`
		// верхнего уровня был бы собран в проект импортирующего файла, то есть
		// подготовка побежала бы ещё раз, внутри самой приёмки.
		{ name: "fixture", testMatch: /fixture\.setup\.ts$/ },
		{
			name: "acceptance",
			testMatch: /g3-005-web\.spec\.ts$/,
			dependencies: ["fixture"],
		},
		{
			// Отдельный проект, а не вторая спека внутри `acceptance`: у G3-006 свой
			// бюджет времени. Сценарий отправляет 101 сообщение штатным путём и ждёт
			// барьер публикации — это минуты, а не секунды, и общий `timeout`
			// пришлось бы поднимать для обеих спек сразу, ослабив G3-005.
			//
			// Зависимость та же: состояние прогона пишет `fixture`, а G3-006 его читает.
			name: "g3-006",
			testMatch: /g3-006-web\.spec\.ts$/,
			dependencies: ["fixture"],
			timeout: 600_000,
		},
		{
			// Отдельный проект и по той же причине, что у G3-006. Бюджет свой, и
			// он длиннее: сценарии держат по три страницы сразу (человек и его
			// вторая вкладка), ждут публикацию через Kafka, а 8б ещё и наполняет
			// ленту до перерастания окна — то есть до того момента, когда
			// объявленное прочтение перестаёт двигаться вместе с приходящим.
			//
			// Зависимость та же: состояние прогона пишет `fixture`.
			name: "g3-007",
			testMatch: /g3-007-web\.spec\.ts$/,
			dependencies: ["fixture"],
			timeout: 900_000,
		},
	],
})
