# Инструменты разработки. Рабочие зависимости ставятся отдельно, из
# requirements.txt: проверки API импортируют FastAPI, и без него сборка
# падает на сборе тестов.
#
# Правило «модульный уровень не ходит по сети» это не нарушает: оно про базу,
# Kafka и Redis, а не про библиотеки в процессе. Первая редакция перепутала
# эти две вещи, и на чистом клоне тесты не запускались — локально было
# незаметно, потому что рабочие зависимости уже стояли в окружении.
pytest==8.3.4
ruff==0.8.6
# TestClient из FastAPI построен на httpx.
httpx==0.28.1
